/**
 * Canonical frontend reward amounts used across UI copy and mocks.
 */

export const TOKEN_LAUNCH_CREDITS = 250;

export function formatCreditReward(amount: number): string {
  return `+${amount.toLocaleString()}`;
}

