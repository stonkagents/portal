# Assets

Place **imported** images, SVGs, or other assets here. Use `@/assets/...` in code.

For **static** files (favicon, robots.txt, public images), use `public/` at project root so they are served at the root URL.
