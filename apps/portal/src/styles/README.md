# Styles

- **variables.css** – Design tokens (colors, typography, spacing). Import in `app/globals.css`.
- Add theme files here (e.g. `theme.css`, `dark.css`) when needed.
- Prefer CSS variables for theming; use Tailwind or CSS modules per team choice.
