/**
 * TEMPORARY hard-codes for initial UI testing only.
 *
 * Every export here must carry a `// TEMP:` comment stating why it exists and
 * what replaces it. `scripts/check-temporary.mjs` fails the production build
 * while this file exports anything. Keep it empty whenever you can.
 */

export {};
